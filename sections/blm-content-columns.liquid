<div class="blm-content-columns" {{ section.shopify_attributes }}>
  <div class="blm-container">
    {% if section.settings.heading != blank %}
      <h2 class="blm-content-columns__heading">{{ section.settings.heading }}</h2>
    {% endif %}

    <div class="blm-content-columns__grid">
      {% for block in section.blocks %}
        {% if block.type == 'column' %}
          <div class="blm-content-columns__col" {{ block.shopify_attributes }}>
            {% if block.settings.eyebrow != blank %}
              <span class="blm-eyebrow">{{ block.settings.eyebrow }}</span>
            {% endif %}
            {% if block.settings.title != blank %}
              <h3 class="blm-content-columns__col-title">{{ block.settings.title }}</h3>
            {% endif %}
            {% if block.settings.content != blank %}
              <div class="blm-content-columns__col-body rte">{{ block.settings.content }}</div>
            {% endif %}
          </div>
        {% endif %}
      {% endfor %}
    </div>

    {% if section.settings.cta_1_label != blank or section.settings.cta_2_label != blank %}
      <div class="blm-content-columns__cta-row">
        {% if section.settings.cta_1_label != blank %}
          <a href="{{ section.settings.cta_1_url }}" class="button button--primary">{{ section.settings.cta_1_label }}</a>
        {% endif %}
        {% if section.settings.cta_2_label != blank %}
          <a href="{{ section.settings.cta_2_url }}" class="button button--secondary">{{ section.settings.cta_2_label }}</a>
        {% endif %}
      </div>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Content columns",
  "tag": "section",
  "class": "blm-content-columns-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading (optional)"
    },
    {
      "type": "header",
      "content": "Optional CTA row (below columns)"
    },
    {
      "type": "text",
      "id": "cta_1_label",
      "label": "Primary CTA label (optional)"
    },
    {
      "type": "url",
      "id": "cta_1_url",
      "label": "Primary CTA link"
    },
    {
      "type": "text",
      "id": "cta_2_label",
      "label": "Secondary CTA label (optional)"
    },
    {
      "type": "url",
      "id": "cta_2_url",
      "label": "Secondary CTA link"
    }
  ],
  "blocks": [
    {
      "type": "column",
      "name": "Column",
      "settings": [
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow (optional)"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Column title",
          "default": "Column title"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Column content",
          "default": "<p>Column content goes here.</p>"
        }
      ]
    }
  ],
  "max_blocks": 4,
  "presets": [
    {
      "name": "Content columns",
      "blocks": [
        { "type": "column" },
        { "type": "column" }
      ]
    }
  ]
}
{% endschema %}
